# Copy to infra/openclaw.env (git-ignored) and fill in.
# Optional — the stack starts without it; OpenClaw just won't have providers
# or channels connected.
#
# AI provider (pick what you use)
ANTHROPIC_API_KEY=""
OPENAI_API_KEY=""

# Messaging channels — see the OpenClaw docs for the full list.
# TELEGRAM_BOT_TOKEN=""
# SLACK_BOT_TOKEN=""
